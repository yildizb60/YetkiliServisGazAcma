@{
    ViewData["Title"] = "Yeni Personel";
    ViewData["PanelTitle"] = ViewData["Title"];
    ViewData["PanelSubtitle"] = "Admin Paneli";
    ViewData["ActiveMenu"] = "Personeller";
    Layout = "~/Views/Shared/_PanelLayout.cshtml";

    var sirketler = ViewBag.Sirketler as List<YetkiliServisGazAcma.Entities.Dag_Sirket> ?? new();
    string formAdSoyad = ViewBag.FormAdSoyad ?? "";
    string formEmail = ViewBag.FormEmail ?? "";
    string formTelefon = ViewBag.FormTelefon ?? "";
    int formSirketId = ViewBag.FormSirketId is int s ? s : 0;
}

<div class="df-admin-workspace df-admin-editor df-person-create-workspace">
    <section class="df-page-panel df-admin-hero">
        <div class="df-panel-head">
            <div>
                <div class="df-panel-kicker"><i class="bi bi-person-plus"></i> Personel hesabı</div>
                <div class="df-panel-title">Yeni Personel</div>
                <div class="df-panel-subtitle">Personelin iletişim, şirket ve ilk giriş bilgilerini tanımlayın.</div>
            </div>
            <a class="df-btn df-btn-secondary df-btn-sm" href="/AdminPanel/personeller"><i class="bi bi-arrow-left"></i> Personellere Dön</a>
        </div>
    </section>

    <section class="df-admin-form-card">
        <div class="df-admin-form-body">
            @if (ViewBag.Hata != null)
            {
                <div class="df-alert df-alert-danger">@ViewBag.Hata</div>
            }

            <form method="post" action="/AdminPanel/personeller/ekle">
                @Html.AntiForgeryToken()

                <section class="df-admin-form-section">
                    <div class="df-admin-form-section-head">
                        <span><i class="bi bi-person-vcard"></i></span>
                        <div>
                            <h3>Personel Bilgileri</h3>
                            <p>Personelin kimlik ve iletişim bilgilerini girin.</p>
                        </div>
                    </div>
                    <div class="df-admin-form-grid">
                        <div class="df-admin-field is-half">
                            <label>Ad Soyad *</label>
                            <input name="adSoyad" value="@formAdSoyad" autocomplete="name" required />
                        </div>
                        <div class="df-admin-field is-half">
                            <label>E-Posta *</label>
                            <input type="email" name="email" value="@formEmail" autocomplete="email" required />
                        </div>
                        <div class="df-admin-field is-half">
                            <label>Telefon</label>
                            <input name="telefon" value="@formTelefon" autocomplete="tel" />
                        </div>
                    </div>
                </section>

                <section class="df-admin-form-section">
                    <div class="df-admin-form-section-head">
                        <span><i class="bi bi-building-lock"></i></span>
                        <div>
                            <h3>Şirket ve Giriş Bilgileri</h3>
                            <p>Personelin çalışacağı şirketi ve ilk giriş şifresini belirleyin.</p>
                        </div>
                    </div>
                    <div class="df-admin-form-grid">
                        <div class="df-admin-field is-half">
                            <label>Şirket *</label>
                            <select name="SirketId" required>
                                <option value="">Seçiniz</option>
                                @foreach (var sirket in sirketler)
                                {
                                    <option value="@sirket.Id" selected="@(formSirketId == sirket.Id)">@sirket.SirketAdi</option>
                                }
                            </select>
                        </div>
                        <div class="df-admin-field is-half">
                            <label>Geçici Şifre *</label>
                            <input type="password" name="sifre" autocomplete="new-password" required />
                            <small class="df-admin-field-note">En az 6 karakter, en az bir küçük harf ve bir rakam.</small>
                        </div>
                    </div>
                </section>

                <div class="df-admin-form-actions">
                    <a class="df-btn df-btn-secondary df-btn-sm" href="/AdminPanel/personeller"><i class="bi bi-x-lg"></i> Vazgeç</a>
                    <button type="submit" class="df-btn df-btn-primary df-btn-sm"><i class="bi bi-person-check"></i> Personeli Oluştur</button>
                </div>
            </form>
        </div>
    </section>
</div>
